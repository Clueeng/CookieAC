package fr.clue.cookieac.check;

public enum CheckType {
    // this will be used for exempts probably
    MOVEMENT,
    COMBAT,
    PLAYER; // since this one is not so self-explanatory, player hacks are hacks that mess with the player behaviour, whether that is GUI, block placement etc...
}
